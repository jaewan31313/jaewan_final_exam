#<오픈소스프로그래밍 기말 프로젝트>
#
# ● 아래의 코드를 수정 혹은 프로그래밍하여 문제를 해결하시오.
# ● 문제의 점수는 각각 표시되며, 부분점수가 존재합니다.
#
# 학번 : 20201815 이름 : 전재완

import os
import time

# Q.1 10점
#
# 문자열 my_string과 target이 매개변수로 주어질 때,
# target이 문자열 my_string의 부분 문자열이라면 1을,
# 아니라면 0을 return 하는 solution 함수를 작성하시오.
#
# 제한사항
# 1 ≤ my_string 의 길이 ≤ 100
# my_string 은 영소문자로만 이루어져 있습니다.
# 1 ≤ target 의 길이 ≤ 100
# target 은 영소문자로만 이루어져 있습니다.
import sys


def solution(my_string, target):
    if target in my_string:
        return 1
    else:
        return 0


def lowercase_english(prompt):
    while True:
        user_input = input(prompt)
        if user_input.islower() and 1 <= len(user_input) <= 10:  
            return user_input
        else:
            print("길이가 맞지않거나 영어 소문자가 아닙니다")


my_string_input = lowercase_english("(my string) 영어 소문자로 입력해주세요: ")
target_input = lowercase_english("(target) 영어 소문자로 입력해주세요: ")


result = solution(my_string_input, target_input)
print(result)  # 결과 출력

sys.exit()